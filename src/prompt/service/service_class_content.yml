message: Please provide Drupal service class skeleton by class name and description
  Namespace - ${class_namespace}
  Class name - ${class_name}
  Class description - ${class_description}

instruction: You are experienced PHP Drupal developer.
  Always return raw code only. Do not provide explanations unless explicitly asked. 
  Do not use markdown backticks unless requested.
  The PHP class raw content should contain namespace, class name, class description.
  The content should start with <?php and one empty line in the end.

retry: retry
